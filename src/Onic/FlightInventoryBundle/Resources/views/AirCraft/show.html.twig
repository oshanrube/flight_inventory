{% extends 'panel.html.twig' %}

{% block panel %}
    <table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered" id="datatable-example">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ aircraft.id }}</td>
        </tr>
        <tr>
            <th>Airline</th>
            <td>{{ aircraft.idairline }}</td>
        </tr>
        </tbody>
    </table>

    <div class="bg-default content-box text-center pad20A mrg25T">
        {{ form_start(delete_form) }}
            <a href="{{ path('aircraft_index') }}" class="btn btn-border btn-alt border-default font-default">back</a>
            <a href="{{ path('aircraft_edit', { 'id': aircraft.id }) }}" class="btn btn-border btn-alt border-default font-default">Edit</a>
            <button class="btn btn-primary">Delete</button>
        {{ form_end(delete_form) }}
    </div>
{% endblock %}
